//! DO NOT REMOVE
const prompt = require("prompt-sync")({ sigint: true });

/**
  Example usage to prompt a user: 
  const name = prompt('What is your name?');
  console.log(`Hey there ${name}`);

  All user input will be read as a String, so in order to treat user input as numbers, you’ll need to convert the input:
*/
/////////////////////////////////////////////  WRITE YOUR CODE BELOW!!!  //////////////////////////////////////
//Hello, you!
//PUT YOUR FUNCTION HERE!
// function hello(name) {
//     if (name.length > 0) {
//         console.log(`hello, ${name}`)
//     }
//     else {
//         console.log(`hello, world`)
//     }
// }
// hello('');

//PROMPT
// const name = prompt("What is your name? ")
//CALL YOUR FUNCTION

// function madlib(one, two) {
//     return `${one}'s favorite subject in school is ${two}.`
// }
// console.log(madlib('Conrad', 'Say Gex'))

// tipAmount = (price, quality) => {
//     if (quality == 'good') {
//         return price * .2
//     } else if (quality == 'fair') {
//         return price * .15
//     } else {
//         return price * .1
//     }
// }
// console.log(tipAmount(500, 'bad'))

// tipAmount = (price, quality) => {
//     if (quality == 'good') {
//         return price + (price * .2)
//     } else if (quality == 'fair') {
//         return price + (price * .15)
//     } else {
//         return price + (price * .1)
//     }
// }
// console.log(tipAmount(500, 'good'))
// let price = prompt("Enter price: ");
// let quality = prompt("Enter service quality good, fair, or poor: ");
// let split = prompt("Enter number of party: ");
// tipAmount = (price, quality, split) => {
//     if (quality === "good") {
//         return (price + price * 0.2) / split;
//     } else if (quality === "fair") {
//         return (price + price * 0.15) / split;
//     } else {
//         return (price + price * 0.1) / split;
//     }
// };
// console.log(tipAmount(price, quality, split))
